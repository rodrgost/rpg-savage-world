{
  "name": "rpg-adaptavel",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm -w backend run dev",
    "dev:frontend": "npm -w frontend run dev",
    "dev:backend": "npm -w backend run dev",
    "dev:backend:clean": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$conns = netstat -ano | Select-String ':3100'; if ($conns) { $pidList = $conns | ForEach-Object { ($_.ToString().Trim() -split '\\s+')[-1] } | Sort-Object -Unique | Where-Object { $_ -ne '0' }; foreach ($procId in $pidList) { Stop-Process -Id $procId -Force -ErrorAction SilentlyContinue } }; npm -w backend run dev\"",
    "dev:emulators": "npx firebase emulators:start --only firestore",
    "build": "npm run build:backend && npm run build:frontend",
    "build:all": "npm run build",
    "build:frontend": "npm -w frontend run build",
    "build:backend": "npm -w backend run build",
    "validate": "npm run build:all",
    "start": "npm -w backend run start"
  }
}
