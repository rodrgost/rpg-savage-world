/**
 * Arquivo mantido apenas por compatibilidade de imports existentes.
 * As regras de dados do Savage Worlds foram removidas do system prompt —
 * o LLM decide livremente o resultado de cada ação via narrativa.
 * @deprecated Este arquivo pode ser removido quando todos os imports forem atualizados.
 */
